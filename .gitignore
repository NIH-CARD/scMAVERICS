############################################
# OS generated files 
############################################

._*
.DS_Store*
.Spotlight-V100
.Trashes

############################################
# FILES TO IGNORE    
# *.extension = ignore all files with this particular extension
############################################

*.ipynb
slurm-*.out

############################################
# WHOLE FOLDERS TO IGNORE
# my_folder/* = ignore all files inside my_folder 
#   - but keeps an empty my_folder instead
#   - which allows you to skip files listed in the `EXCLUSIONS` section
#   - also prevents folder not found errors
############################################

.cache
.snakemake
atlas/*
data/*
figures
envs/*
input/*
logs/*
notebook/*
old_pipelines
plots/*
scripts/logs
scripts/old_scripts
snakemake_profile/*

############################################
# EXCLUSIONS
# !my/file/path.txt = use this to keep any files from the `WHOLE FOLDERS TO IGNORE` section
#   - ie. README.md which should be kept in that folder
############################################

#? Q) Should we keep a figures folder? What about an output folder?